spring:
  application.name: productmanagement
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
  jpa.hibernate.ddl-auto: create-drop
server.servlet.context-path: /api
